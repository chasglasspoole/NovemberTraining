import org.openqa.selenium.WebDriver;

import foundation.PageObject;

public class HomePage extends PageObject {

	protected HomePage(WebDriver driver, String baseUrl) {
		super(driver, baseUrl);
	}
}